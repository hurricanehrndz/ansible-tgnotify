---
postfix_config_file: /etc/postfix/main.cf
posfix_mydomain: "{{ ansible_domain }}"

postfix_service_state: started
postfix_service_enabled: true

postfix_inet_interfaces: loopback-only
postfix_inet_protocols: ipv4
